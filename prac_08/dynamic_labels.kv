BoxLayout:
    orientation: 'vertical'

    BoxLayout:
        orientation: 'vertical'
        id: main

    Button:
        text: 'Add Label'
        on_press: app.add_label()
        size_hint_y: 0.25

    Button:
        text: 'Clear Labels'
        on_press: app.clear_labels()
        size_hint_y: 0.25

